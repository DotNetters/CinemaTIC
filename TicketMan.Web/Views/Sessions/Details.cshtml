@model Session

@{
    ViewBag.Title = Literals.SessionDetails;
}

<h2>@Literals.SessionDetails</h2>
<hr />

<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.TimeAndDate)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.TimeAndDate)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Status)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Status)
        </dd>

    </dl>
</div>
<hr />
<div>
    @Html.ActionLink(Literals.Edit, "Edit", new { id = Model.Id }, new { @class = "btn btn-sm btn-primary" })
    @Html.ActionLink(Literals.Back, "Index", null, new { @class = "btn btn-sm btn-default" })
</div>

@section Scripts {
    <script type="text/javascript">
            $(function () { setActiveMenuItem("SessionsMenuItem"); });
    </script>
}
